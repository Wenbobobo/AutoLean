interface:
  display_name: "AutoLean Benchmarks"
  short_description: "Run evidence-gated AutoLean benchmark comparisons"
  default_prompt: "Use $run-autolean-benchmarks to execute and compare a frozen AutoLean benchmark plan."
